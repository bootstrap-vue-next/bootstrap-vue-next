import type {TransitionMode} from '@/types'

// Props
export interface Props {
  appear?: boolean
  mode?: TransitionMode
  noFade?: boolean
  transProps?: Record<string, unknown>
}
// Emits

// Other
