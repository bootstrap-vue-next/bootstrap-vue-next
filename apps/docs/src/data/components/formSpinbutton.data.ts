import type {ComponentReference} from '../../types'

export default {
  load: (): ComponentReference[] => [
    {
      component: 'BFormSpinbutton',
      props: {},
      emits: [],
      slots: [],
    },
  ],
}
